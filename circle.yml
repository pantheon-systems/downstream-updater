machine:
  hosts:
    localhost: 127.0.0.1

dependencies:
  pre:
    - git clone https://github.com/sstephenson/bats.git;  sudo ./bats/install.sh /usr/local

test:
  override:
    - ./local/bin/bats tests

